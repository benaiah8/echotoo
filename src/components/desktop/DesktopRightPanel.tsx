import DesktopMarketingPanel from "./DesktopMarketingPanel";

/**
 * Right-hand desktop column beside the phone preview (web desktop shell only).
 * Marketing content only: policy/help URLs use the full-page legal layout instead.
 */
export default function DesktopRightPanel() {
  return <DesktopMarketingPanel />;
}
