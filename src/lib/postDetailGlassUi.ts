/**
 * Post detail modal only — keeps the top actions pill and bottom comment pill
 * the same width without touching HomeTopBar / BottomTab.
 */
export const POST_DETAIL_GLASS_PILL_WIDTH_CLASS = "w-[92%] max-w-[640px]";

export const POST_DETAIL_GLASS_PILL_MAX_WIDTH_PX = 640;
